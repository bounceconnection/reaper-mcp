# Smithery configuration file for REAPER MCP

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({
      command: 'reaper-mcp-server',
      args: []
    })
  exampleConfig: {}
